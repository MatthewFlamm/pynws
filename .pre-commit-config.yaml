ci:
    autofix_prs: false
    autoupdate_schedule: monthly

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.7
    hooks:
      - id: ruff
        args:
          - --fix
        files: '^(pynws|tests)/'
      - id: ruff-format
        files: '^(pynws|tests)/'
